name: RDP Enhanced (32GB Simulation & Unlimited Runtime)

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    # استخدام Windows 2022 (قد يمنح أداءً أفضل قليلاً، لكن الذاكرة الفعلية محدودة بـ GitHub)
    runs-on: windows-2022
    timeout-minutes: 4320  # 3 أيام (محاولة لتجاوز الحد الـ 6 ساعات، لكن GitHub قد يفرض حداً أعلى)

    steps:
      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Secure Password
        run: |
          Add-Type -AssemblyName System.Security
          $charSet = @{
            Upper   = [char[]]('A'..'Z')
            Lower   = [char[]]('a'..'z')
            Number  = [char[]]('0'..'9')
            Special = [char[]]('!@#$%^&*()_+-=[]{};:,.<>?/')
          }
          $rawPassword = @()
          $rawPassword += $charSet.Upper | Get-Random -Count 4
          $rawPassword += $charSet.Lower | Get-Random -Count 4
          $rawPassword += $charSet.Number | Get-Random -Count 4
          $rawPassword += $charSet.Special | Get-Random -Count 4
          $password = -join ($rawPassword | Sort-Object { Get-Random })
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          echo "RDP_CREDS=User: RDP | Password: $password" >> $env:GITHUB_ENV

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i `"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
            $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
            Start-Sleep -Seconds 5
            $retries++
          }
          if (-not $tsIP) {
            Write-Error "Tailscale IP not assigned. Exiting."
            exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Verify RDP Accessibility
        run: |
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
            Write-Error "TCP connection to RDP port 3389 failed"
            exit 1
          }
          Write-Host "TCP connectivity successful!"

      - name: Install Performance Tools (Simulate 32GB Environment)
        run: |
          # تثبيت أدوات لتحسين الأداء (لا تؤثر على الذاكرة الفعلية)
          choco install googlechrome firefox 7zip cpu-z hwinfo -y
          # عرض معلومات النظام الحقيقية
          systeminfo | Select-String "Total Physical Memory"
          wmic os get TotalVisibleMemorySize

      - name: Maintain Connection (Unlimited Runtime)
        run: |
          Write-Host "`n=== RDP ACCESS ==="
          Write-Host "Address: $env:TAILSCALE_IP"
          Write-Host "Username: RDP"
          Write-Host "Password: $(echo $env:RDP_CREDS | Select-String -Pattern 'Password: (.+)' | ForEach-Object { $_.Matches.Groups[1].Value })"
          Write-Host "==================`n"
          Write-Host "NOTE: Actual RAM is limited by GitHub runner (~7GB)."
          Write-Host "This workflow simulates a 32GB environment by installing monitoring tools."
          Write-Host "To truly get 32GB, you need a self-hosted runner with that hardware."
          Write-Host "`nKeeping runner alive indefinitely (or until cancelled)."
          while ($true) {
            Write-Host "[$(Get-Date)] RDP Active - Use Ctrl+C in workflow to terminate"
            # إعادة محاولة الاتصال إذا انقطع (تجديد الـ Tunnel)
            & "$env:ProgramFiles\Tailscale\tailscale.exe" status
            Start-Sleep -Seconds 300
          }

      - name: Fallback Keep-Alive (if Tailscale drops)
        if: failure()
        run: |
          Write-Host "Tailscale might have disconnected. Attempting to re-connect..."
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          while ($true) {
            Write-Host "[$(Get-Date)] Reconnected - RDP still active"
            Start-Sleep -Seconds 300
          }
